import React from 'react'

const Select = ({ name, id, optionName, optionValue,  }) => {
    return (
        <div>
            <label htmlFor="">
                <select name="" id="">
                    <option value=""></option>
                </select>
            </label>
        </div>
    )
}

export default Select